You are Gemini 3.8 Flash high assisting Codex. Own ONLY rust/crates/hermes-gateway/src/inbound_text_context.rs. Read AGENTS.md. Rust 2021 stable 1.95.0.

Port pure sender/reply context from actual gateway/run.py _prepare_inbound_message_text, plus gateway/session.py neutralize_untrusted_inline_text. Use existing session::SessionSource, session::is_shared_multi_user_session, platform_base_types::MessageEvent and the correct Platform enum from actual SessionSource. Implement neutralize_untrusted_inline_text(&str, max_chars: usize), prepend_sender_context(message_text: &str, event: &MessageEvent, source: &SessionSource, group_sessions_per_user: bool, thread_sessions_per_user: bool), prepend_reply_context(message_text: &str, event: &MessageEvent, source: &SessionSource, discord_tools_loaded: bool). Sender context includes shared-user prefix, Slack verified user ID, then channel context. Reply context includes Discord triggering ID (only when tool gate true) then reply snippet of 500 Unicode code points with own-message wording. Follow exact ordering and source guard truthiness. Neutralize must preserve Python split/strip semantics, CR/LF/control handling, 240-character default at caller, Unicode code point truncation and the peculiar max_chars<3 slicing behavior. No network or config lookup; Discord tool readiness is supplied by caller. No @ reference expansion yet.

Preserve byte-exact prompt text; use \u{2014} escapes where source has em dashes. Tests cover malicious display names/newline collapse, shared versus private context, Slack source identity, channel block ordering, reply Unicode truncation and Discord gate. Do not modify other files, run cargo, commit, spawn helpers, inspect user credentials/config or call model APIs. Parent owns integration and Python differential fixtures. Finish with a concise report.
