Review the completed Rust main-provider static API-key pool checkpoint adversarially. Work only in rust/analysis/main-provider-pool-review-claude.md. Do not edit production code, tests, PORT.md, INDEX.md, helper tasks, or goldens. Read the current working diff and the live Python sources plus rust/tools/main-provider-pool-goldens.json. Focus on correctness and security defects, not style. Check startup precedence, provider/profile isolation, exact failed-key attribution, persistence before retry, ordinary and pre-exhausted 429 request counts, auth/billing/usage/upstream classification, bounded full-pool traversal, clone and concurrent-turn behavior, cancellation, blocking I/O, fresh clients, per-entry endpoint changes, header leakage, stable bodies and prompts, tool-round carryover, final error propagation, and the deferred OAuth/non-chat/fallback boundary. Identify any mismatch between helper prose and executable evidence. For every finding, give severity, exact file and line, a concrete failing scenario, and the smallest safe fix. If no defect exists in an area, say what evidence supports that conclusion. Run focused read-only tests if useful. Use no em dash characters. End with a prioritized verdict.
