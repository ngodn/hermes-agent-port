You are Claude Opus 4.8 medium helping Codex. Own ONLY rust/crates/hermes-gateway/src/image_routing.rs. Read actual agent/image_routing.py and project instructions. Rust 2021 stable 1.95.0. Port _coerce_capability_bool, _supports_vision_override, _coerce_mode, _explicit_aux_vision_override, and decide_image_input_mode. Keep public names without leading underscore except explicit_aux_vision_override can use original minus underscore. coerce_capability_bool(&serde_json::Value)->Option<bool>; supports_vision_override(cfg:&Value,provider:&str,model:&str,requested_provider:&str)->Option<bool>; coerce_mode returns enum ImageInputMode {Auto,Native,Text}; explicit_aux_vision_override(&Value)->bool. decide_image_input_mode should be async, supplied a trait VisionCapabilityLookup: Send+Sync whose async lookup(provider,model,cfg,requested_provider)->anyhow::Result<Option<bool>> represents the real _lookup_supports_vision call. Return Result<ImageInputMode> and preserve exceptions: do not silently swallow lookup errors at this layer. The trait is temporary I/O seam for parent's next capability implementation, not proof of live runtime integration. Do NOT implement the lookup network waterfall or URL/key resolution here yet.

Exact behavior matters: top-level override precedes provider model config; requested provider > runtime provider > configured provider; custom: alias expansion; candidate-first legacy provider list walk; supports_vision present null suppresses vision alias; true bool/int0/1 only (floats rejected); Python string coercion for odd config values and strip/lower semantics. Keep helper private if needed. Use existing dependencies only. Tests inline in this file with focused source-contract comments, no sibling test files. No excessive narrative docs. No literal em dashes. Do not edit main.rs or manifests or trackers, run cargo, commit, spawn agents or inspect credentials. Parent adds differential fixtures and integration. End concise report.
