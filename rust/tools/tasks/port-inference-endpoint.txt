Implement the Python agent/image_routing.py _resolve_inference_base_url and _resolve_inference_api_key behavior in existing rust/crates/hermes-gateway/src/image_routing.rs ONLY. Read existing helpers first and reuse Python coercion/whitespace behavior. Expose a small explicit runtime struct holding provider, base_url, api_key (borrowed strings is fine) and two functions accepting cfg, provider, runtime. Runtime context must be passed, never process-global. Preserve Python's asymmetry: URL checks provider match, runtime API key does not. Python candidate provider names use a set so document deterministic precedence for ambiguous conflicting entries rather than pretending Python has a stable order. Follow existing style, focused comments and inline tests only. Do not invoke network, cargo, agents, commit or edit any other file. Rust edition 2021, toolchain stable 1.95.0. Parent will generate source oracles and integrate. Report any ambiguous behavior explicitly.
