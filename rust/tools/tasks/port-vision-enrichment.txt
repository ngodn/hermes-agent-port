You are Claude Opus 4.8 medium helping Codex port Hermes. Own ONLY rust/crates/hermes-gateway/src/vision_enrichment.rs. Read project instructions and existing transcription_enrichment.rs conventions. Rust 2021 stable 1.95.0.

Port actual GatewayRunner._enrich_message_with_vision from gateway/run.py and sanitize_context plus its three regexes from agent/memory_manager.py. Expose an async trait VisionBackend: Send + Sync returning anyhow::Result<String> for analyze(path, prompt), preserving JSON parsing and malformed-result/error distinction. Port exact prompt strings, input order including duplicates, success truthiness, missing analysis versus null/non-string failure, captions, per-image exception continuation. Use fancy-regex already installed for sanitizer, with Python whitespace semantics where necessary. Public sanitize_context should be reusable by future memory port. No real provider implementation yet.

Tests MUST be inline in this source file, using #[cfg(test)] mod tests, no sibling test files. Clear module docs and focused comments on contracts/non-obvious behavior, not every statement. No literal em dashes in new comments; preserve source strings through Unicode escapes. Do not edit main.rs, manifests, trackers, other sources. Do not run cargo or commit or spawn agents. Parent integrates and builds independent Python oracle. End with concise report.
