You are Gemini 3.8 Flash high assisting Codex. Own ONLY rust/tools/gen_session_lifecycle_goldens.py and rust/tools/session-lifecycle-goldens.json. Generate reproducible --check fixtures by AST-extracting actual hermes_state.py end_session, reopen_session, promote_to_session_reset, _bump_conversation_generation, and needed hermes_state_common.py schema/constants/_legacy_reset_child_sql. Use SQLite with source schema, deterministic time.time()=100, and a harness _execute_write that runs callback in a real transaction. Do not reimplement method behavior. Seed session rows and apply a list of end/promote/reopen operations, recording selected session columns and conversation_generations after each operation. Expected JSON should normalize model_config by parsing its JSON when valid so formatting is not treated as semantic. Cover first-end-wins, accidental-end promotion vs explicit-boundary preservation, no-op/missing sessions, generation increments only for real keyed reset boundaries, repeated resets after reopen, legacy child marker stamping with same/different/empty keys, existing markers, malformed JSON rollback. Under 100 purposeful cases. Run with mise exec python@3.12.13 -- python and --check. Paths relative to __file__, not machine-specific. No Rust edits, cargo, commits, helpers, credentials, web, or model APIs. Concise comments, no em dashes. Report coverage and any failures honestly.
