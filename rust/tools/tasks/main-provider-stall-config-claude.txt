You own the timeout-configuration and liveness-ownership lane for ordinary native main-provider chat-completions. Do not edit Rust production code, Rust tests, PORT.md, AGY artifacts, or tool-truncation artifacts.

Build on rust/analysis/main-provider-client-rebuild-seam-claude.md. Inspect current Python configuration schema, defaults, environment bridges, setup/config loading, runtime scaling rules, and tests for request timeouts and streaming stale deadlines. Inspect the Rust config schema and native client construction to identify the exact existing or missing ownership path. Determine whether the Rust port should preserve an environment-only compatibility input, add a config.yaml key, derive a fixed frozen value, or defer pending a broader config port. Include first-byte versus inter-chunk semantics and how pre-visible versus post-visible timeout outcomes reach the existing retry dispatcher.

Write only rust/analysis/main-provider-stall-config-claude.md. Give exact live file and line references, a concrete minimal recommendation, coercion/default/scaling tables, public-seam red tests, cache and concurrency implications, and explicit non-chat/OAuth/auxiliary deferrals. Do not duplicate AGY's tool-truncation contract. Do not use em dash characters in authored files.
