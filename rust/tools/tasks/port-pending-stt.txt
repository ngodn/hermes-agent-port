You are the Claude Opus 4.8 medium helper for Codex. Own ONLY rust/crates/hermes-gateway/src/pending_stt.rs. Rust 2021 on stable 1.95.0. Read AGENTS.md and source before implementing.

Port the pending voice preprocessing state from gateway/run.py: _pending_event_audio_paths, _transcribe_pending_audio_event_once, _echo_pending_stt_transcripts_once, _prepare_clarify_reply_text. Also inspect gateway/platforms/base.py _invalidate_pending_stt_cache and merge_pending_message_event for invalidation semantics. Use existing platform_base_types::MessageEvent and inbound_media::event_media_is_stt_input.

Represent transient state explicitly as a PendingStt struct owning cached output (Option distinguishes unprepared from a prepared null/empty result) and echo count. Do not add arbitrary metadata keys to MessageEvent or change its type. APIs should accept mutable PendingStt plus event references; use generic async callback closures for transcription and echo so actual consumers can supply their existing backends. Do not implement new STT providers or network APIs. Use anyhow::Result and std::future::Future as needed. Preserve source edge cases: no audio does not cache; caller-provided empty text overrides event text; cached text ignores later caller text; STT path filter does NOT add is_audio; failed echo is nonfatal and count advances before sends; invalidate cached text/transcripts but retain echo count when another note merges; repeated identical transcripts remain distinct; clarify text uses successful trimmed transcripts.

Tests must execute async APIs with counting callbacks and cover reuse, error retry, append/invalidate, duplicate transcript echoes, failure accounting, disabled echo/missing adapter equivalence, no audio and empty text. Existing standalone modules may use allow(dead_code) while runner callers are pending. Keep comments concise. No invented behavior or stubs. Do not modify other files, run cargo, commit, spawn helpers, inspect user credentials/config or call model APIs. Codex owns integration and independent validation. Finish when the file is written and report scope and limitations.
