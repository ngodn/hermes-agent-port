This is a short, single-agent validation task. Do not spawn subagents. Own only one read-only Python runtime validation report for the Rust retry-notice checkpoint. Do not edit Python or Rust production code, tests, PORT.md, INDEX.md, progress files, or existing analysis/oracle files. Write only rust/analysis/main-provider-retry-trace-validation-agy.md.

Run the current Python AIAgent conversation loop with patched provider I/O and zero-duration waits for exactly four ordinary chat-completions cases: (1) HTTP 500 then success, (2) HTTP 500 terminal exhaustion with no fallback, (3) ordinary HTTP 503 overload through one fallback and terminal exhaustion, and (4) HTTP 500 request-format rejection through one fallback and terminal exhaustion. Capture the exact ordered status records buffered, cleared, flushed, and emitted, including exact retry numerators, fallback-attempt lines, durable model-switch lines, and final terminal status. Also state the provider call count per route. Use real current loop methods and patch boundaries, not a copied simulator. If a case cannot be driven within the task time, cite the exact live source branch and mark only that case source-traced. Run only focused existing tests needed to validate the harness. Finish the report before extra investigation. Do not use literal em dash characters in the authored report; encode them as `\u2014` when quoting runtime text.
