You are Claude Opus 4.8 medium, assisting Codex. Own ONLY rust/crates/hermes-gateway/src/pending_messages.rs. Read AGENTS.md and the actual gateway/platforms/base.py merge_pending_message_event and BasePlatformAdapter._merge_caption, plus pending_stt.rs and platform_base_types.rs. Rust 2021 stable 1.95.0.

Port complete pending-event merge behavior. Define PendingMessage { pub event: MessageEvent, pub stt: PendingStt } with new(event), and free merge_pending_message_event(pending: &mut HashMap<String, PendingMessage>, session_key: &str, event: MessageEvent, merge_text: bool). Exact existing Python behavior: photo+photo merges metadata/captions even with empty media; media merges pad inline flags (preserve overlong flags), concatenate MIME arrays as-is (do NOT repair missing MIME arrays), invalidate derived STT but retain echo count; preserve original event identity/reply fields when merging; PHOTO dominates merged type, otherwise TEXT upgrades to incoming type; merge_text only joins TEXT/TEXT; replacement resets STT. Reuse PendingStt::invalidate. Implement merge_caption as a helper with Python strip behavior including U+001C..U+001F. Distinct captions use blank lines and exact trimmed equality, not substring matching. Do not modify other modules. Main agent owns integration and differential fixtures.

Add focused executable tests, including merging a previously transcribed/echoed PendingMessage and proving only the cache invalidates, plus replacement resets the state. Use pending_stt async functions (their callbacks now return Result<(Option<String>, Vec<String>)>). Do not run cargo, commit, spawn helpers, inspect user credentials/config or call other model APIs. No em dashes in new prose. Finish when the one file is complete with a concise report of scope and gaps.
