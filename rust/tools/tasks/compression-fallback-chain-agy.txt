Own the Python-contract oracle lane for the next Rust checkpoint. Do not edit Rust source files and do not implement production wiring.

Audit the authoritative Python behavior for auxiliary.compression.fallback_chain, focusing on the ordinary summary request failure path, not only the special stalled-worker retry. Read agent/auxiliary_client.py, agent/context_compressor.py, agent/conversation_compression.py, provider/config resolution helpers, and relevant tests. Determine exact parsing/coercion, entry order, invalid-entry handling, route identity skipping, per-entry timeout, api_mode/transport aliases, credential lookup precedence, model defaults, minimum context filtering, and final fallback to the main conversation route. Distinguish behaviors that the current native chat-completions-only Rust transport can implement safely from Python-only transports or credential mechanisms that must remain deferred.

Create these files only:

1. rust/analysis/compression-fallback-chain-contract-agy.md, a concise source-cited contract, edge-case table, recommended bounded Rust checkpoint, and explicit deferrals.
2. rust/tools/gen_compression_fallback_chain_goldens.py, a deterministic source-executed generator that imports/calls real Python functions where practical instead of copying logic.
3. rust/tools/compression-fallback-chain-goldens.json, generated by that script.

The corpus must cover enough cases to pin structural entry acceptance, order, timeout coercion, api_mode/transport selection, explicit key/key_env behavior, exact failed-route skipping where the Python helper can be invoked safely, and exhaustion. Include --check support matching the repository's other generators. Run the generator and its --check mode. Do not touch INDEX.md or PORT.md. Never include live secret values in output. Avoid em dash characters in all new prose.
