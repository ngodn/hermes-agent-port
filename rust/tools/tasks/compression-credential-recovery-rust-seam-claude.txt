Inspect the existing Rust code only for the smallest production seam that can connect the already-ported credential_pool.rs and auth_store.rs behavior to native auxiliary compression discovery and request-time recovery. This is a Rust ownership and concurrency review lane. Do not edit production code, Python oracle files, PORT.md, INDEX.md, or existing analysis files.

Read rust/crates/hermes-gateway/src/credential_pool.rs, auth_store.rs, secret_scope.rs, compression_discovery.rs, native_agent.rs, main.rs, provider_registry.rs, and relevant tests. Determine how profile-scoped pool state should be loaded, selected, persisted, and shared without violating immutable per-conversation prompts or leaking credentials across profiles. Analyze whether candidates should freeze keys, retain a pool handle, rebuild clients on 401, or use another existing abstraction. Address lock lifetimes across await, selection side effects, 401/402/429 transitions, client eviction, bounded retries, cache ownership, shutdown, and how missing credentials could become available to an existing conversation. Identify exact reusable functions and gaps. Do not analyze the Python source contract, because AGY owns that lane.

Write only rust/analysis/compression-credential-recovery-rust-seam-claude.md. Include a concrete proposed data model, call flow, ownership table, race analysis, minimal test matrix, and any recommendation that should be rejected as unsafe or speculative. Do not implement anything.
