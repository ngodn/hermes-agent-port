Audit the exact current Python contract for auxiliary compression credential-pool selection and request-time credential recovery. This is an independent Python-contract lane. Do not edit Rust production code, PORT.md, INDEX.md, or existing analysis files.

Trace the authoritative implementation in agent/auxiliary_client.py, agent/credential_pool.py, hermes_cli/auth.py, and directly required provider auth helpers. Cover only behavior relevant to auxiliary compression: pool presence versus fallback to singleton/env credentials, selection strategy and cooldown/exhaustion eligibility, runtime key/base URL/model identity, 401 refresh or rotation, 402/429 accounting, persistence timing and failure behavior, provider health, poisoned-client eviction, the maximum request/retry budget, and profile/root auth-store shadowing. Clearly separate API-key rotation from OAuth refresh and identify behaviors that cannot be ported without provider-specific transports.

Create these new files only:
1. rust/analysis/compression-credential-recovery-contract-agy.md, with exact source locations, state transitions, invariants, failure semantics, and explicit unknowns.
2. rust/tools/gen_compression_credential_recovery_goldens.py, a deterministic source-executed oracle that imports and exercises current Python helpers where practical. It must not access real credentials or the network. Include a --check mode against the checked-in JSON.
3. rust/tools/compression-credential-recovery-goldens.json, generated by that script.

Run the generator, report exact case and section counts, and verify --check. Keep every claim grounded in current source or executed behavior. Do not recommend a Rust architecture beyond listing Python-owned state and contracts.
