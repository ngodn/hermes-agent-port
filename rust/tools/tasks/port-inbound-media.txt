You are the Claude Opus 4.8 (medium) helper for the main Codex agent working on the Hermes Rust rewrite. Read AGENTS.md and rust/PORT.md. Rust edition 2021, stable toolchain (1.95.0 here).

Own ONLY rust/crates/hermes-gateway/src/inbound_media.rs. Port the five actual gateway/run.py functions _event_media_type_at, _event_media_is_image, _event_media_is_audio, _event_media_is_stt_input, _event_media_is_video using the existing platform_base_types::MessageEvent and MessageType. Read their complete bodies and existing types first. Include a classify_media helper that returns image_paths, transcription_paths, audio_file_paths, video_paths, faithfully matching the classification loop in GatewayRunner._prepare_inbound_message_text, with an explicit pending_stt_prepared flag. This is a bounded Tier 2 slice; network enrichment and model resolution remain separate.

Add meaningful unit tests for explicit MIME precedence, unknown MIME fallback, VOICE versus AUDIO versus DOCUMENT STT, mixed attachments, pending STT caching, and case/whitespace handling. Source Python is the specification; preserve quirks. Do not invent fallback behavior or change existing types. The parent independently generates Python differential fixtures and integrates your file.

Do not modify any other file, run cargo, commit, spawn helpers, call model APIs, or inspect credentials/user config. Use Read/Grep/Glob and Write/Edit tools. If required behavior cannot fit existing types, report the precise limitation instead of stubbing it. Finish with a concise report of functions ported, edge cases and remaining integration. No em dashes in new prose/comments.
