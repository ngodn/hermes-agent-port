You own a separate response-stall and primary-client rebuilding lane for the Rust port. Do not edit Rust production code, Rust tests, PORT.md, AGY artifacts, or length-continuation artifacts.

Inspect current Python and Rust behavior for ordinary main-provider chat-completions when a request or stream fails in a way that retires, closes, or rebuilds the primary HTTP/OpenAI client. Focus on run_agent.py client retirement and replacement, transport stream errors, retry ownership, shared-client cache and file-descriptor safety, and the current reqwest ownership in rust/crates/hermes-gateway/src/native_agent.rs. Do not analyze text length continuation except where needed to distinguish it from a stalled stream.

Write only rust/analysis/main-provider-client-rebuild-seam-claude.md. The report must give exact live file and line references, verified trigger classes and non-triggers, concurrency and ownership invariants, the smallest safe Rust seam, whether rebuilding reqwest is actually required for parity, public-seam red tests, and explicit deferrals for non-chat, OAuth, and auxiliary clients. Cite relevant focused Python tests and report any commands you run. Do not duplicate AGY's length-continuation contract. Do not use em dash characters in authored files.
