Own only the Rust configuration-model lane for auxiliary compression fallback chains. Do not edit main.rs, native_agent.rs, PORT.md, INDEX.md, Python source, generators, or golden JSON.

First audit the authoritative Python fallback-chain configuration behavior in agent/auxiliary_client.py and related helpers. Then modify only rust/crates/hermes-gateway/src/compression_auxiliary.rs and create rust/analysis/compression-fallback-chain-config-claude.md.

Implement a narrow typed representation for ordered auxiliary.compression.fallback_chain entries that the primary lane can later resolve into native clients. Preserve the existing primary Config behavior. Parse entries in source order, reject non-object entries and entries without a nonempty provider, retain optional model/base_url/api_key/key_env/api_key_env/api_mode/transport/extra_body/reasoning_effort/max_output_tokens/timeout fields with Python-compatible coercion where the existing module already has helpers, and expose only the minimum crate-visible API needed by startup. Do not resolve provider profiles or credentials outside direct/key-env lookup because startup owns those dependencies. Keep per-entry timeouts independent and apply the compression timeout floor only if that is what the ordinary Python summary fallback path actually does. Add focused Rust unit tests in the same file. If a requested detail is not supported by the source, document and omit it instead of guessing.

Run rustfmt on the edited Rust file and the focused hermes-gateway test target for compression_auxiliary. Record commands and results in the report. Avoid em dash characters in all new prose.
