Review the current uncommitted Rust implementation of native Nous OAuth compression discovery and recovery. This is a post-implementation security, concurrency, and prompt-cache review only. Do not edit production code, tests, or the AGY oracle. Write rust/analysis/nous-oauth-recovery-review-claude.md with findings ordered by severity and exact file/line references.

Inspect the live diff and the current Python source yourself. Focus on issues that can cause token loss, refresh-token replay, lock inversion, stale peer adoption, profile/root pollution, persistence-after-use, credential leakage, SSRF or redirect leakage, unsafe operator/network URL mixing, cancellation after a single-use POST, unbounded retries, stale HTTP clients, changed summary request bytes, or broken discovery ordering. Verify the real HTTP and filesystem tests are sensitive to those invariants. Call out missing production behaviors separately from correctness defects in the implemented slice.

Do not repeat the Python behavior-corpus lane owned by AGY. Your earlier seam report incorrectly described (nous, device_code) as borrowed/sanitized. The live Rust is_borrowed function explicitly exempts that pair, so begin from the corrected fact that owned Nous device-code secrets are persisted in the pool. Also verify, rather than assume, shared filenames, URL allowlists, and active-provider behavior. Do not expose credential values and do not use em dashes.
