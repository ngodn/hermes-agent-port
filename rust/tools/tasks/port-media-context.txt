You are Gemini 3.8 Flash high working with Codex. Own ONLY rust/crates/hermes-gateway/src/media_context.rs. Read AGENTS.md. Rust 2021, stable 1.95.0, existing dependencies only.

Port pure text builders from gateway/run.py: _build_media_placeholder (reuse inbound_media classifiers), _build_document_context_note, plus the audio/video context-note literals inside _prepare_inbound_message_text. For audio/video builder parameters accept already-resolved display_name and agent_path as &str, not raw host paths. Path translation and filename sanitization are a separate caller concern, do not duplicate or approximate them. Function names build_media_placeholder(event: &MessageEvent), build_document_context_note(display_name, agent_path, mtype, content_inlined: bool), build_audio_context_note(display_name, agent_path), build_video_context_note(display_name, agent_path). All return String.

Preserve exact Python output text, including punctuation, spaces and attachment order. If Python output has em dash characters, encode those with Rust \u{2014} escapes to preserve byte parity without adding literal em dashes in source prose. Use concise tests with meaningful ordering, missing MIME precedence and inlined/non-inlined document cases. Codex independently executes Python to generate golden fixtures and integrates. Do not run cargo, modify any other file, commit, spawn helpers, inspect user credentials/config, or invoke other model APIs. Read actual source, not earlier maps. Finish when the one file is written and report the exact functions and any limitations.
