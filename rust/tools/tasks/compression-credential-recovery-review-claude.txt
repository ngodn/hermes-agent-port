Review the current uncommitted Rust implementation for native auxiliary compression API-key pool recovery. This is a post-implementation correctness and security review, not a reimplementation task.

Inspect the diff and the exact Python behavior in agent/auxiliary_client.py, agent/credential_pool.py, and hermes_cli/auth.py. Focus on:

1. profile/root isolation and whether raw credentials can cross conversations or profiles,
2. lock lifetimes, especially any lock held across await,
3. auth-store read/modify/write races, cooldown merge semantics, symlink and permission behavior,
4. exact 401, payment-like 402/429, ordinary 429, duplicate-key, stale-id, and bounded retry behavior,
5. poisoned reqwest client replacement,
6. prompt/message byte stability and tool-free compression,
7. whether production startup actually selects pool credentials before env credentials,
8. regressions in the pre-existing frozen discovery ordering and two-candidate budget.

Do not edit production code. Write a concise severity-ranked report to rust/analysis/compression-credential-recovery-review-claude.md. Every finding must cite exact current Rust and Python symbols or lines, explain the runtime consequence, and propose the smallest fix. Explicitly say when a suspected issue is not reproducible. End with a ship/no-ship verdict and list the tests you inspected.
