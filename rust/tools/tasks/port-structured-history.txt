You are Claude Opus4.8 medium assisting Codex. Own ONLY rust/crates/hermes-gateway/src/session_db.rs. Rust2021 stable1.95.0. Port structured content storage/replay using actual hermes_state.py SessionDB._encode_content / _decode_content and _CONTENT_JSON_PREFIX. Parent adds hermes_core::Message optional content_parts:Option<Vec<ContentPart>> and fn model_content(&self)->serde_json::Value (text string fallback or array of typed text/image_url parts). Keep HistoryMessage fields unchanged (role:String, content:String storage representation); ADD method model_content(&self)->Value decoding prefix \0json: like Python incl malformed payload raw-string fallback and valid scalar prefixed JSON. ADD public encode_message_content(&Value)->String for string/raw vs array/object prefix. Existing safe scalar limitations explain tests, inputs to begin_turn are string/array. begin_turn must persist encode_message_content(&msg.model_content()) through existing append_message path, preserving existing SQL schema and write critical section; no network work in transaction or changes to lease semantics. No content string auto-detection without sentinel. Tests inline ONLY. Verify actual sqlite reopen/replay of image parts and malformed/plain JSON-looking strings, ASCII/Unicode. Do not edit other modules/Cargo/docs, run cargo, commit, spawn helpers. Parent builds end-to-end HTTP tests and integrates. No literal em dashes in comments. Report remaining gaps, including canonical Python api_content sidecar if out of scope. All tests inline.
