Audit the live Python main-conversation provider fallback contract and build a deterministic source-executed oracle for the next Rust checkpoint. Work only in rust/analysis/main-provider-fallback-contract-agy.md, rust/tools/gen_main_provider_fallback_goldens.py, and rust/tools/main-provider-fallback-goldens.json. Do not edit Rust production code, existing tests, PORT.md, INDEX.md, or any other file. Read run_agent.py, agent/conversation_loop.py, agent/chat_completion_helpers.py, agent/agent_init.py, hermes_cli/fallback_config.py, hermes_cli/runtime_provider.py, and the credential-pool code. This is the ordinary main turn, not auxiliary compression. Pin the exact parsing and merge order of fallback_providers plus legacy fallback_model, entry validation and credentials, trigger reasons, primary-pool-before-fallback ordering, upstream-rate-limit behavior, iteration and retry budgets, chain traversal bounds, provider/profile mismatch isolation, endpoint and header reconfiguration, request-body and system-prompt stability, tool-loop carryover, whether fallback sticks across later rounds and turns, restoration/reset behavior, and final error propagation. Separate chat-completions static API-key behavior that Rust can safely port now from OAuth, non-chat transports, dynamic plugins, and unrelated recovery. Execute live source helpers instead of copying expected values by hand. The generator must support write and --check modes, sort JSON deterministically, report section and case counts, and contain no secrets or em dash characters. Include explicit caveats for anything the oracle does not truly prove. End the report with a prioritized minimum viable native contract.
