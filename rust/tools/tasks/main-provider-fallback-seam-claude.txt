Map the narrowest safe Rust seam for ordinary main-conversation fallback_providers and legacy fallback_model activation after the active provider and its credential pool cannot recover. Work only in rust/analysis/main-provider-fallback-seam-claude.md. Do not edit production code, tests, existing analysis, PORT.md, INDEX.md, helper tasks, or goldens. Read the current Rust tree at commit d87226dbc9, especially native_agent.rs, main.rs, credential_pool.rs, compression_auxiliary.rs, conversation_agent.rs, and the existing compression main-fallback implementation. Read the corresponding live Python main-turn fallback sources, but do not duplicate the AGY behavior-oracle assignment. Focus on ownership and interface design: what fallback route state must freeze per conversation, which existing parser/client builder can be reused without inheriting auxiliary-only semantics, how both streaming and tool-loop requests enter one activation seam, how the primary pool must fully recover before cross-provider fallback, route-specific clients and headers, shared clone state, fallback stickiness across tool rounds and later turns, retry and chain bounds, cancellation and blocking I/O, usage attribution, prompt-cache stability, and how reset or a new conversation restores the configured primary. Compare at least two plausible internal designs, recommend one based on depth and locality, give concrete type and method sketches, list a red-first local HTTP integration test, and identify any prerequisite that makes this checkpoint unsafe. Use no em dash characters. End with a concise implementation order and explicit deferred OAuth, non-chat, plugin, and dynamic-refresh scope.
