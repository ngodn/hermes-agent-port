You are Gemini 3.8 Flash high assisting Codex. Own ONLY rust/tools/gen_session_peer_goldens.py and rust/tools/session-peer-goldens.json. Build a reproducible --check oracle for hermes_state.py SessionDB.find_latest_gateway_session_for_peer using AST-extracted actual source SQL/method, actual reason constants from hermes_state_common.py, and SQLite temp/in-memory tables. Do not reimplement selection rules in Python. A minimal harness may provide _read_ctx, _own_profile_name and _session_row_dict (dict(row) is enough for selection assertions). Fixtures must contain seed session/message rows, lookup arguments, owner profile, and expected selected session ID or null, so Rust tests can reproduce them. Cover exact-key precedence; message-bearing versus empty rows; durable recency; recoverable end reasons; strict reset boundary timestamps; peer tuple mismatches; profile-owner fencing; disabled fallback when chat_id/chat_type absent. Use source-compatible schema columns needed by the SQL, including system_prompts/system_prompt_hash/system_prompt. Keep case count purposeful, under 150. Run with mise exec python@3.12.13 -- python and verify --check. No Rust edits, no cargo, no commits, no helpers, no credentials, no web/model APIs. Use comments explaining fixture cases, no em dashes. Finish with a concise report listing coverage and limitations.
