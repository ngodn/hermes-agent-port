You are Gemini 3.8 Flash high assisting Codex. Own ONLY rust/crates/hermes-gateway/src/image_references.rs. Read actual agent/image_routing.py extract_image_refs plus _LOCAL_IMAGE_PATH_RE, _IMAGE_URL_RE and extension set. Port full extraction using existing fancy-regex, explicit home:&Path argument for ~/ expansion, real filesystem is_file checks, ignored fenced and inline code spans, dedup and exact output ordering, original URL matching quirks and punctuation strip. Proposed API extract_image_refs(text:&str,home:&Path)->(Vec<String>,Vec<String>). This is local filesystem work, no network URL validation. Preserve Python regex Unicode \w (letters/numbers/underscore, excludes combining marks unlike Rust), \b and IGNORECASE oddities on extensions, POSIX home path behavior. Do not invent a stricter URL parser: use actual source semantics. Inline tests ONLY in the implementation file; focused comments on non-obvious behavior. No new dependencies, no edits to main/manifests/docs, no cargo, no commits, no spawning helpers. Parent handles oracle corpus and builds. No literal em dashes in comments. End concise report.
