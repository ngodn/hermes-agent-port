Implement managed local vision capability in ONLY new rust/crates/hermes-gateway/src/managed_capabilities.rs. Read full hermes_cli/local_runtime/capabilities.py and dependencies _state_endpoint/_pid_alive in endpoint.py, is_managed_endpoint in growth.py, staged_models/staged_model_ids/models_dir/assets_dir in bootstrap.py, state_path in supervisor.py, catalog find_entry_for_model and AssetFile.local_name/QuantVariant.model_id. Use explicit default Hermes root PathBuf and explicit catalog serde_json::Value in a ManagedCapabilities instance; caller supplies loaded catalog, do not invent catalog-refresh policy. Implement actual filesystem staging including complete split GGUF sets, actual read of runtimes/llamacpp/server.json (verify exact path!), PID liveness on POSIX, real async /health and /props with 3s connect/read timeouts and source urllib redirect policy. Expose is_managed_provider(provider, base_url) async and managed_model_supports_vision(model) async. Preserve live false wins, catalog unknown None, no projector false, projector existence, staging before requests, source path/name rules and truthiness. Tests inline, real temporary homes + loopback HTTP, no server launches/kills except abort test server handles; never signal real recorded PIDs except harmless POSIX kill(pid,0). Windows must never use kill0; report unported platform behavior. Rust2021 stable1.95.0, no cargo, no dependencies, no other file edits, no agents/commits. Concise comments, no em dashes. Parent generates Python fixtures and integration. Do not conflate this with boot/supervisor lifecycle or native catalog refresh, which remain separate.
