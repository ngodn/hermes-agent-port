You are Gemini 3.8 Flash high assisting Codex. Own ONLY rust/crates/hermes-gateway/src/native_image_content.rs. Port actual agent/image_routing.py _sniff_mime_from_bytes, _guess_mime, _file_to_data_url, build_native_content_parts with REAL disk reads, base64 encoding and PNG transcoding, not a callback-only scaffold. Rust2021 stable1.95.0. Parent adds image0.25.10 crate (png jpeg gif webp bmp tiff ico pnm tga qoi hdr exr ff features), base640.22.1, mime_guess2.0. Official API docs checked: https://docs.rs/image/0.25.10/image/ (load_from_memory, DynamicImage::write_to with Cursor and ImageFormat::Png); base64 Engine STANDARD.encode; mime_guess::from_path(path).first_raw().

Public API: pub struct NativeImageOptions<'a> { pub read_policy: &'a crate::file_read_safety::FileReadPolicy, pub accepted_mimes: &'a [&'a str] }. Other helper authors implement FileReadPolicy::check_read(&self,path:&str)->anyhow::Result<()>; call that before fs::read. pub const UNIVERSALLY_SUPPORTED_MIMES:&[&str] = png/jpeg/gif/webp. pub fn build_native_content_parts(user_text:&str,image_paths:&[String],image_urls:&[String],options:&NativeImageOptions<'_>)->(Vec<serde_json::Value>,Vec<String>). pub fn sniff_mime_from_bytes(&[u8])->Option<&'static str>. Real helper functions file_to_data_url(path:&Path,options)->Option<String>, guess_mime(path:&Path,raw:Option<&[u8]>)->String. Preserve byte-signature order (SVG bytes lowercase and bytes.lstrip ASCII only), accepted image formats pass unchanged even corrupt, unsupported sniffed formats transcode when decoder available, otherwise skip; no resizing or invented upload limits. Managed runtime narrower accepted set supplied by caller, no process env. Image size stays original, pixel/alpha preservation tests on BMP/TIFF plus pass-through PNG. Note HEIC/AVIF optional decoder gap, do not silently claim those supported. mime_guess differs from OS Python mimetypes: document as known remaining mapping parity work rather than claim exact for all extensions. Tests inline ONLY with proper comments, no other source files. Do not modify Cargo/main/trackers, run cargo, commit or spawn agents. Parent supplies independently executed Python fixtures and integration. Do not inspect credentials or user config.
